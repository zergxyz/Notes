how to use conda to create env and remove env:
``` 
conda create --name abc
conda remove --name myenv --all
```

how to deal with spacy lib can not find 'en' modal? 
```
python -m spacy download en 
```
